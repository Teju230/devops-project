pipeline {
    agent any

    stages {

        stage('Clone') {
            steps {
                echo 'GitHub Connected Successfully'
            }
        }

        stage('Build Docker Image') {
            steps {
                bat 'docker --version'
            }
        }

    }
}
